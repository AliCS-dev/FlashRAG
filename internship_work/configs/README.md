# Configs

Store custom YAML configuration files here when they belong to internship experiments rather than the base FlashRAG project.

Suggested naming:

```text
exp_001_custom_retrieval.yaml
exp_002_retrieval_error_analysis.yaml
```

For reproducibility, copy the exact config used for an experiment into that experiment folder as well.
